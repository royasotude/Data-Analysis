numpy-q1 : The replace_odd function takes a NumPy array as input and replaces all odd elements with -1. 
numpy-q2 : The p2p_distance function takes an array as input and, without using any loops, calculates the Euclidean distance between one point and another point.
